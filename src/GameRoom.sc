;;; Sierra Script 1.0 - (do not remove this comment)
;
;	GAMEROOM.SC
;
;	This is a custom room class. Customize it however you want.
;	If you decide not to use it, it will consume no heap.
;

(script# GAMEROOM)
(include game.sh)
(use Main)
(use Game)

(class GameRoom kindof Room)